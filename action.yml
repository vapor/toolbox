name: 'Vapor'
description: 'Vapor Toolbox'
inputs:
  command:
    description: 'Command to run'
    required: true
    default: '--help'
runs:
  using: 'docker'
  image: 'action.Dockerfile'
  args:
    - ${{ inputs.command }}
